var document = document || null;
if (document) { document.addEventListener('click', (b) => { function n(a, e) { a.className = a.className.replace(u, '') + e; } function p(a) { return a.getAttribute('data-sort') || a.innerText; } var u = / dir-(u|d) /; let c = /\bsortable\b/; b = b.target; if (b.nodeName === 'TH') { try { let q = b.parentNode; let f = q.parentNode.parentNode; if (c.test(f.className)) { let g; let d = q.cells; for (c = 0; c < d.length; c++)d[c] === b ? g = c : n(d[c], ''); d = ' dir-d '; b.className.indexOf(' dir-d ') !== -1 && (d = ' dir-u '); n(b, d); let h = f.tBodies[0]; let k = [].slice.call(h.rows, 0); let r = d === ' dir-u '; k.sort((a,
  e) => { let l = p((r ? a : e).cells[g]); let m = p((r ? e : a).cells[g]); return isNaN(l - m) ? l.localeCompare(m) : l - m; }); for (var t = h.cloneNode(); k.length;)t.appendChild(k.splice(0, 1)[0]); f.replaceChild(t, h); } } catch (a) {} } }); }
